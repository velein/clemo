import { css } from '@emotion/core'
import styled from '@emotion/styled'
import { SlideStyledProps } from '.'

// const isVisible = ({ visible }: SlideStyledProps) =>
//     visible &&
//     css`
//         display: block;
//     `

export const SlideStyled = styled.div`
    border: 1px solid #000;
    padding: 30px;
`
